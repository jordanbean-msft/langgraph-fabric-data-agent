# Console environment variables
# Copy to .env and fill your values before running: uv run langgraph-fabric-console

# Foundry / Azure OpenAI settings
AZURE_OPENAI_ENDPOINT=https://your-foundry.services.ai.azure.com/api/projects/your-project
AZURE_OPENAI_RESPONSES_DEPLOYMENT_NAME=gpt-5.4
AZURE_OPENAI_API_VERSION=2025-11-15-preview

# MCP server settings (JSON list)
# Console/local mode typically does not need oauth_connection_name.
MCP_SERVERS=[{"name":"fabric","description":"Fabric analytics MCP server","url":"https://api.fabric.microsoft.com/v1/mcp/workspaces/<workspace-id>/dataagents/<agent-id>/agent","scope":"https://api.fabric.microsoft.com/.default","oauth_connection_name":"","timeout_seconds":120,"poll_interval_seconds":2}]

# Optional: Entra app for device-code credential fallback (local auth)
# Leave blank to use the Azure CLI (az login) credential instead
MICROSOFT_APP_ID=
MICROSOFT_TENANT_ID=

# Runtime settings
LOG_LEVEL=INFO
# Per-logger overrides: LOG_LEVEL_OVERRIDE=langgraph_fabric_core.graph:DEBUG,azure.core:WARNING
LOG_LEVEL_OVERRIDE=
