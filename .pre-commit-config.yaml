repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.8
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: local
    hooks:
      - id: pytest
        name: pytest
        entry: uv run pytest -W error -W ignore:Exception\ ignored\ in:pytest.PytestUnraisableExceptionWarning
        language: system
        pass_filenames: false
        stages: [pre-push]
